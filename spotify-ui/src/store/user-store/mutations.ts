import { MutationTree } from 'vuex';
import { UserStoreInterface } from './state';

const mutation: MutationTree<UserStoreInterface> = {
  // setUser (state, user: User) {
  //   state.user = user;
  // }
};

export default mutation;
